import { VibrateDirective } from './vibrate.directive';

describe('VibrateDirective', () => {
  it('should create an instance', () => {
    const directive = new VibrateDirective();
    expect(directive).toBeTruthy();
  });
});
